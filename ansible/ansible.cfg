#config for ansible
# host_key_checking is false to avoid connection problems. We do not have a certificate authority.
[defaults]
inventory = inventory.ini
host_key_checking = False
roles_path = ./roles

[ssh_connection]
# Required for become_user (e.g. postgres) to work without temp file permission errors
pipelining = True